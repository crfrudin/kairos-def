/**
 * Componente "casca" da UI de edição do Perfil.
 *
 * BLOCO 6.1:
 * - Sem regra de negócio
 * - Sem integração ainda
 * - Sem imports shadcn por enquanto (para não depender de quais componentes já estão gerados)
 *
 * BLOCO 6.2:
 * - Introduziremos shadcn (Form/Input/Select/Switch/etc) e integraremos via Public API da feature profile.
 */
export function ProfileEditShell() {
  return null;
}
