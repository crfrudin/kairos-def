/**
 * KAIROS · FASE 1 · BLOCO 6
 * UI de edição do Perfil (camada de input)
 *
 * Regras:
 * - UI não decide nada.
 * - Nenhuma regra crítica aqui.
 * - Integração obrigatória via UC-01 GetProfile e UC-02 UpdateProfile (feature profile).
 *
 * Observação:
 * Este módulo é intencionalmente "UI-only".
 */
export * from './contracts/profile-edit-contracts';
export * from './components/ProfileEditShell';
