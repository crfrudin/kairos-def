# Auth / Application / Use-cases

Este diretório será preenchido **use-case por use-case**, conforme a Fase 9.

Regras:
- Cada use-case deve depender apenas de ports (ex.: IAuthRepository).
- Retorno padrão: Result (sem exceptions para falhas esperadas).
- Sem chamadas diretas ao Supabase nesta camada.
